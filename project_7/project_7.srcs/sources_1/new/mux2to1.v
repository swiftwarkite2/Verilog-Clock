`timescale 1ns / 1ps
//////////////////////////////////////////////////////////////////////////////////
// the building block module used to select between bcd digit bits for display
//////////////////////////////////////////////////////////////////////////////////

module mux2to1(
output wire y,
input wire d0, d1, s
    );
assign y = (!s && d0)||(s && d1);
endmodule
